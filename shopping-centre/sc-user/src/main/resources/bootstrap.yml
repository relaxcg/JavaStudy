spring:
  application:
    name: sc-user
  cloud:
    nacos:
      config:
        namespace: sc-dev
        file-extension: yaml
        server-addr: localhost:8848
        shared-configs:
          - data-id: sc-common.yaml
            refresh: true
