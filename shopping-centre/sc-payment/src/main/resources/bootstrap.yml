spring:
  application:
    name: sc-payment
  cloud:
    nacos:
      config:
        namespace: sc-dev
        file-extension: yaml
        server-addr: localhost:8848
        shared-configs:
          - data-id: sc-common.yaml
            refresh: true
